import authenticateToken from './authenticateToken';
import trimBodyString from './trimBodyString';
import validatorMiddleware from './validatorMiddleware';

export default {
  authenticateToken,
  trimBodyString,
  validatorMiddleware
}